      <link href="/css/style.css" rel="stylesheet"/>
    
    <div class="mb-5 pt-3">
        <a href="/dashboard/create/"><button id="create-post" class="button">Create Post</button></a>
        <br>
        <br>

{{#if posts.length}}
    </div>

    {{#each posts as |post|}}
        {{> post-info post}}
        
            <a href="/dashboard/edit/{{post.id}}"><button class="button mb-5 ml-5 is-warning">edit post</button></a>
        </div>
    {{/each}}

{{else}}
<h4 class="noPosts">You currently have no Posts. Click "Create Post" to create your first Post!</h4>

{{/if}}

