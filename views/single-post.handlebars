{{> post-info post}}


{{#if loggedIn}}
<form class="enterComment">
  <div>
    <textarea name="enterComment" placeholder="Enter your comment..."></textarea>
  </div>

  <div>
    <button class="button" type="submit">Add Comment</button>
  </div>
  
</form>
{{/if}}

{{> comments post.comments}}

{{#if loggedIn}}
<script src="/js/comment.js"></script>
{{/if}}

